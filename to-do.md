# TO-DO — дальнейшие шаги

Дорожная карта от скелета к рабочему боту. Логика — в `design-doc.md`, отложенные
решения и принятые риски — в `known_issues.md`. Порядок фаз учитывает зависимости.

**Статус кода:** каркас готов (FSM-роутинг, DI через `createServices`/`AppDeps`,
провижининг, надёжность, юнит-тесты). **Провайдеры (Yandex + LLM через Vercel AI
SDK/Groq) и флоу добавления слова (§4) реализованы и проверены вживую** (Telegram +
Yandex + Groq + Postgres); typecheck/lint/тесты зелёные. Остальные флоу — заглушки.

---

## Фаза 0. Прероквизиты — поднять окружение ✅ (локально)
- [x] Получить креды: `BOT_TOKEN` (@BotFather), `YANDEX_DICT_KEY`, `GROQ_API_KEY`
      (+ `LLM_PROVIDER`), `OWNER_CHAT_ID`, `OWNER_TZ`, `DATABASE_URL`.
- [x] Поднять PostgreSQL (docker `english-bot-pg`), заполнить `.env`.
- [x] `npm run db:migrate` — схема применена.
- [x] Smoke: `/start` → провижининг (`users`+`settings`) и whitelist проверены.
- [ ] Для прод-деплоя повторить с боевыми кредами и БД (Фаза 7).

## Фаза 1. Провайдеры (реализация за интерфейсами) ✅ проверено вживую
- [x] **YandexDictionary.lookup** (`services/dictionary/yandex.ts`): реальный GET (native
      fetch); смысл = `tr` (не `def`), фильтр хвоста по `tr.fr`, `tr.mean`→глосс, один ответ
      (`tr.syn` не берём); ошибки по `body.code` (HTTP-статус врёт), таймаут 8с. Маппер
      юнит-тестируется. (§4)
  - [x] «замок» подтверждён живьём (1 `def`, 2 `tr`); закрыт разбиением по `tr` (known_issues №2).
- [x] **LLM через Vercel AI SDK** (`services/llm/aisdk.ts` + `model.ts`): `generateExample` +
      `fallbackTranslate`, `generateObject` + zod, таймаут 20с. Провайдер по env `LLM_PROVIDER`
      (Groq, `openai/gpt-oss-120b`). (§2, §4)
- [ ] (Опц.) кэш/ретраи на сетевые вызовы (429/таймауты).

## Фаза 2. Флоу добавления слова (§4) ✅ проверено вживую
grammY conversations v2 (`features/add.ts`): replay-модель, все side-effects в
`conversation.external`, время через `conversation.now()`. Весь флоу — ОДНО
сообщение, правится по шагам (`editMessageText`).
- [x] Конвейер: детект языка → словарь → (если >1 смысла) выбор смысла (варианты в
      тексте, номерные кнопки) → LLM-пример → подтверждение → сохранение (`russian`,
      `english` — один ответ, пример, `source`, `next_review = завтра`).
- [x] Фоллбэк редких слов: пометка «автоперевод, проверь», `source=fallback`. (§4)
- [x] Подтверждение: «Другой пример» (регенерация, всегда) + «Изменить перевод»
      (только фоллбэк — там перевод от LLM). Словарный перевод на добавлении не
      правится; омограф закрыт выбором смысла (§3/§10).
- [x] `ensureUser` на входе записи (покрыть DB-reset-mid-run).
- [x] Мягкий дедуп: показываем существующую карточку «возможно, уже есть» (known_issues №4).
- [x] Юнит-тесты на маппинг словаря и сборку карточки.
- [ ] Известное ограничение: команда (`/...`) во время ожидания ввода проглатывается
      (есть кнопка «Отмена»). Робастная обработка — Фаза 7.

## Фаза 3. Флоу повторения (§5) + проводка SRS ← следующий шаг
- [ ] Выборка: `next_review <= сегодня(TZ)` ASC; размер N; остаток → завтра.
- [ ] Карточка: спойлеры (HTML `<tg-spoiler>` — без боли с экранированием MarkdownV2),
      кнопки «Помню / Не помню».
- [ ] SRS на нажатие: `promote`/`reset` + `nextReviewDate(todayInTz, idx)`
      (`lib/srs` + `lib/dates`). Линейный проход; финал «Повторение на сегодня закончено».
- [ ] Режим `review`: вход/выход; коллизия — любой текст → повторить карточку. (§8)
- [ ] `/repeat` — ручной старт, помечает сегодняшнее плановое как пропущенное.

## Фаза 4. Флоу тестирования (§6)
- [ ] Выборка: `next_review > сегодня`; `ORDER BY last_tested ASC NULLS FIRST,
      next_review DESC`; размер = `test_count`.
- [ ] RU→EN: промпт → ждём ответ → `isCorrect` (детерминизм). Верно → дальше
      (+`last_tested`); неверно → «Не правильно!» + `reset(next_review)` + `last_tested`.
- [ ] Аварийный выход «Показать ответ / Пропустить» = провал.
- [ ] Цикл: гонять неправильные до верного.
- [ ] Режим `test`: коллизия (русский ввод → «только на английском»), таймаут 20 мин. (§8)
- [ ] Фоллбэк-слова тестируются наравне со всеми (принятый риск, known_issues №5).

## Фаза 5. Планировщик (§5, §7)
- [ ] Реальная джоба: ежеминутно найти юзеров, у кого `review_time` == сейчас в их TZ
      и сегодня повторения ещё не было → отправить «Регулярное повторение» + первую карточку.
- [ ] Идемпотентность «сегодня уже отправляли/проходили» + «ручной старт пропускает плановый».
- [ ] Rate-limit ~1 msg/сек на чат; обработка `429` / `retry_after`.

## Фаза 6. Настройки (§9)
- [ ] `review_count` (макс = размер колоды), `review_time`, `test_count` — чтение/запись
      из меню.
- [ ] (Будущее) 4-й пункт — выбор таймзоны (known_issues №6); env `OWNER_TZ` станет seed-дефолтом.

## Фаза 7. Сквозное / hardening / прод
- [ ] FSM-переходы: ставить `session.mode` при входе в review/test, сбрасывать в `idle`
      на завершении/таймауте.
- [ ] Отложенное плановое повторение, если пришло во время теста → очередь, запуск после. (§8)
- [x] conversations v2: порядок установки (`session` → `conversations` →
      `createConversation`) — сделано в Фазе 2, проверено вживую.
- [ ] Build для прода (tsup/tsc) + процесс-менеджер; решить webhook vs long-polling при деплое.
- [ ] Деплой (хостинг + БД), бэкапы.
- [ ] При росте/боли: джиттер интервалов (known_issues №1); индекс под тест-выборку (ревью 5d).

## Бэклог / отложено
- [ ] Yandex ToS — перечитать перед коммерцией/мультиюзером (known_issues №3).
- [ ] Мультиюзер: снять whitelist, добавить онбординг (схема уже готова). (§2)
- [ ] Персистентная сессия — только если понадобится (сейчас in-memory, и это правильно).

## UX
- [ ] после сохранения слова удалять сообщение от юзера или служебные вроде "Отменено". Идея – в диалоге должны быть только сообщения "✅ Сохранено (#9)..." и результаты проверок и тестов
