# TO-DO — что осталось

Дорожная карта пройдена до настроек включительно. Что уже сделано — в `git` и
`design-doc.md`; осознанно отложенные решения и принятые риски — в `known_issues.md`.
Здесь оставляем **только то, что ещё нужно или можно сделать**.

**Статус:** каркас, добавление слова (§4), повторение (§5), тест (§6), планировщик
(Фаза 5) и настройки (Фаза 6) реализованы и в репозитории. typecheck/lint/тесты
зелёные (70). Дальше — single-user hardening, прод (отдельно) и отложенный бэклог.

---

## Осталось по коду (single-user скоуп)

1. **У повторения нет «выхода»** — асимметрия с тестом (у теста ленивый таймаут
   20 мин + аварийная «Показать ответ»; у ревью — ничего). `/repeat` во время ревью —
   no-op, `/test` заблокирован. Брошенная сессия (или вручную удалённое флоу-сообщение)
   держит `mode='review'` и молча глотает текст — нельзя добавить слово, пока не
   догрейдишь все карточки или не рестартнёшь бота. Обычно ревью короткое (дожимается
   кнопками), жёсткий капкан — только edge-case с удалённым сообщением, но асимметрию
   стоит закрыть. Фикс: кнопка «Прервать» на карточке (прямой выход) ± ленивый таймаут
   как у теста (самолечение для «вернулся позже / сообщение пропало»).
2. **Команды проглатываются внутри конверсейшна добавления.** Пока флоу добавления
   ждёт ввод, `/команда` либо съедается как ввод (шаг правки перевода), либо удаляется
   (`dropStray` на кнопочных шагах) — но не выполняется. Смягчено кнопкой «Отмена» +
   `dropStray`. Робастно: дать `/test`/`/repeat`/`/settings` прерывать активный
   конверсейшн (grammY conversations v2 это умеет; возни средне).

## Опционально / по мере боли

- **Ретраи/бэкофф для Yandex + LLM** (429/таймаут). Таймауты уже есть (8с/20с); при
  одном юзере лимиты почти недостижимы → низкая ценность сейчас.
- **Джиттер интервалов** (known_issues №1) + **индекс под тест-выборку** (ревью 5d) —
  про масштаб и «лавину» повторений.
- **Правка/удаление слова** (§10) — вернёт `wordById`; сейчас обходится прямым SQL.
  POST-SAVE бэкстоп: поправить/удалить уже сохранённую карточку (неверный перевод/
  пример или не тот смысл, замеченные ПОСЛЕ сохранения). На добавлении неверный
  перевод уже правится кнопкой «Свой перевод».

## Прод / деплой

Код-часть готова (design-doc §2 «Прод-деплой»): tsup-бандл своего кода + внешние
deps (`tsup.config.ts`), multi-stage `Dockerfile` (node:22-slim, non-root, exec →
PID 1 = node, HEALTHCHECK), стартовые миграции (`src/migrate.ts`, fail-fast в
entrypoint), `/healthz` (`src/health.ts`), CI (`.github/workflows/ci.yml`: checks →
GHCR → деплой-вебхук Coolify), `.nvmrc`. Long polling (без Telegram-вебхука /
домена / портов — решено: наши хендлеры легитимно долгие (LLM до 20с), вебхук-модель
ретраила бы апдейты → дубли). Миграции **сквошены в один baseline** перед первым
прод-деплоем (по плейбуку; прод стартует с чистой БД). Контейнер проверен локально
end-to-end (миграции, поллинг, graceful stop). Локальное окружение выведено из
эксплуатации: бот остановлен (токен освобождён под прод), БД очищена и остановлена.

**Задеплоено: LIVE с 2026-06-11**, автодеплой по пушу в `main` работает сквозняком
(CI → GHCR → Coolify-вебхук). Запись проекта — `infra/deploy-playbook.md`.
- [x] Coolify: сеть `english-bot`, managed Postgres 18 (internal), app из GHCR
      (полный путь `ghcr.io/...`!), Consistent Container Names ON, Domains пуст.
- [x] GHCR-пакет переведён в private (репо публичное).
- [x] CI-секреты `COOLIFY_DEPLOY_URL` + `COOLIFY_TOKEN` (отдельный deploy-токен).
- [x] Запись в `infra/deploy-playbook.md` «Deployed projects».
- [ ] Для будущей локальной разработки: ВТОРОЙ бот у BotFather (один токен = один
      поллер; локальный и прод конфликтуют 409-ми) + `docker start english-bot-pg`
      и `npm run db:migrate` (локальная БД пуста).

## Бэклог (отложено по решению)

- **Мультиюзер:** снять whitelist + онбординг (схема уже готова, §2). Тогда же:
  `@grammyjs/auto-retry` для `429` (планировщик/рассылки — сейчас был бы мёртвый код)
  и **TZ из меню** (4-й пункт §9, known_issues №6 — `OWNER_TZ` станет seed-дефолтом).
- **Yandex ToS** — перечитать перед коммерцией/мультиюзером (known_issues №3).
- **Персистентная сессия** — только если понадобится (сейчас in-memory, и это правильно).

## UX (реализованный принцип — на него ссылаются комментарии в коде)

В диалоге остаются только результаты (✅-карточка добавленного слова, итоги тестов);
служебные сообщения, команды и пользовательский ввод чистятся. Каждый флоу
(добавление / повторение / тест / настройки) живёт в одном редактируемом сообщении.
